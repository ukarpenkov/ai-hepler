# Phase 2 — Step 6: Компонент ChatWindow

**Статус:** ✅ Выполнено
**Дата:** 2026-06-21

## Что сделано

1. Создан `packages/web/components/ChatWindow.tsx`:
   - Пропсы: `{ sessionId, initialQuestion }`
   - Состояние: messages, currentInput, isLoading, lastFeedback, lastCoach
   - При монтировании: initialQuestion добавляется как первое сообщение
   - Textarea + кнопка "Отправить"
   - Отправка: user сообщение → sendAnswer → assistant сообщение + feedback
   - FeedbackCard отображается под textarea
   - Автоскролл вниз
   - Кнопка disabled пока isLoading

2. Создан `packages/web/components/ChatWindow.test.tsx` — 3 теста:
   - Рендерит начальный вопрос
   - Отправка ответа добавляет сообщение user
   - После ответа отображается FeedbackCard

3. Исправлен `packages/web/vitest.config.ts` — добавлен path alias `@/`

## Проверки

```
npm run typecheck → ✅
npm run lint → ✅
npm run test → 73 tests passed (22 test files)
```

## Файлы

- `packages/web/components/ChatWindow.tsx` (создан)
- `packages/web/components/ChatWindow.test.tsx` (создан)
- `packages/web/vitest.config.ts` (исправлен)
