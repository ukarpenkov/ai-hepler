# Phase 6 Steps 12-13 — Cloud Build & Secrets Management

**Дата:** 2026-06-21
**Статус:** ✅ Выполнено

---

## Шаг 12 — cloudbuild.yaml

### Что сделано

Создан файл `cloudbuild.yaml` для автоматического деплоя на Cloud Run:

1. **Шаг 1:** Сборка Docker образа (`gcr.io/cloud-builders/docker`)
2. **Шаг 2:** Пуш образа в Container Registry
3. **Шаг 3:** Деплой на Cloud Run с параметрами:
   - Регион: us-central1
   - Порт: 3001
   - Память: 512Mi
   - CPU: 1
   - Max instances: 10

4. **Timeout:** 600s (10 минут)
5. **Logging:** CLOUD_LOGGING_ONLY

### Файлы

- `cloudbuild.yaml` — создан

---

## Шаг 13 — Secrets Management

### Что сделано

1. **Обновлён `src/config.ts`:**
   - Поддержка `OPENROUTER_API_KEY` (приоритет) и `DEEPSEEK_API_KEY` (fallback)
   - Ошибка при старте, если оба ключа отсутствуют
   - `REDIS_URL` — опциональный, дефолт: undefined
   - `PORT` — опциональный, дефолт: 3001

2. **Обновлены маршруты:**
   - `src/api/routes/interview.ts` — использует `config.apiKey`
   - `src/api/routes/job.ts` — использует `config.apiKey`

3. **Обновлены тесты:**
   - `src/utils/__tests__\config.test.ts` — 7 тестов для проверки конфигурации

4. **Создана документация:**
   - `docs/deployment/secrets-setup.md` — инструкции по созданию secrets
   - `docs/deployment/deploy.sh` — скрипт деплоя

### Проверка качества

| Проверка | Результат |
|----------|-----------|
| `npm run typecheck` | ✅ 0 errors |
| `npm run lint` | ✅ 0 warnings |
| `npm run test` | ✅ 158 tests passed |

### Файлы

- `cloudbuild.yaml` — создан
- `src/config.ts` — обновлён
- `src/api/routes/interview.ts` — обновлён
- `src/api/routes/job.ts` — обновлён
- `src/utils/__tests__\config.test.ts` — обновлён
- `docs/deployment/secrets-setup.md` — создан
- `docs/deployment/deploy.sh` — создан
