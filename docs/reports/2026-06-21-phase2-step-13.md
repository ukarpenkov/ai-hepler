# Phase 2 — Step 13: Frontend Environment Configuration

**Status:** ✅ Completed
**Date:** 2026-06-21

## What was done

1. Created `packages/web/.env.example`:
   - NEXT_PUBLIC_API_URL=http://localhost:3001

2. Updated `.gitignore`:
   - Added .next, out, packages/web/.next

3. `packages/web/lib/api.ts` already reads process.env.NEXT_PUBLIC_API_URL with a default

## Checks

```
npm run typecheck → ✅
npm run lint → ✅
npm run test → 73 tests passed (22 test files)
```

## Files

- `packages/web/.env.example` (created)
- `.gitignore` (updated)
