# Phase 2 — Step 6: ChatWindow Component

**Status:** ✅ Completed
**Date:** 2026-06-21

## What was done

1. Created `packages/web/components/ChatWindow.tsx`:
   - Props: `{ sessionId, initialQuestion }`
   - State: messages, currentInput, isLoading, lastFeedback, lastCoach
   - On mount: initialQuestion is added as the first message
   - Textarea + "Send" button
   - Send: user message → sendAnswer → assistant message + feedback
   - FeedbackCard is displayed below textarea
   - Auto-scroll to bottom
   - Button disabled while isLoading

2. Created `packages/web/components/ChatWindow.test.tsx` — 3 tests:
   - Renders the initial question
   - Sending an answer adds a user message
   - After answering, FeedbackCard is displayed

3. Fixed `packages/web/vitest.config.ts` — added path alias `@/`

## Checks

```
npm run typecheck → ✅
npm run lint → ✅
npm run test → 73 tests passed (22 test files)
```

## Files

- `packages/web/components/ChatWindow.tsx` (created)
- `packages/web/components/ChatWindow.test.tsx` (created)
- `packages/web/vitest.config.ts` (fixed)
