# Phase 6 Steps 12-13 — Cloud Build & Secrets Management

**Date:** 2026-06-21
**Status:** ✅ Completed

---

## Step 12 — cloudbuild.yaml

### What Was Done

Created `cloudbuild.yaml` for automated Cloud Run deployment:

1. **Step 1:** Docker image build (`gcr.io/cloud-builders/docker`)
2. **Step 2:** Push image to Container Registry
3. **Step 3:** Deploy to Cloud Run with parameters:
   - Region: us-central1
   - Port: 3001
   - Memory: 512Mi
   - CPU: 1
   - Max instances: 10

4. **Timeout:** 600s (10 minutes)
5. **Logging:** CLOUD_LOGGING_ONLY

### Files

- `cloudbuild.yaml` — created

---

## Step 13 — Secrets Management

### What Was Done

1. **Updated `src/config.ts`:**
   - Support for `OPENROUTER_API_KEY` (priority) and `DEEPSEEK_API_KEY` (fallback)
   - Startup error if both keys are missing
   - `REDIS_URL` — optional, default: undefined
   - `PORT` — optional, default: 3001

2. **Updated routes:**
   - `src/api/routes/interview.ts` — uses `config.apiKey`
   - `src/api/routes/job.ts` — uses `config.apiKey`

3. **Updated tests:**
   - `src/utils/__tests__\config.test.ts` — 7 tests for configuration verification

4. **Created documentation:**
   - `docs/deployment/secrets-setup.md` — secrets creation instructions
   - `docs/deployment/deploy.sh` — deployment script

### Quality Check

| Check | Result |
|-------|--------|
| `npm run typecheck` | ✅ 0 errors |
| `npm run lint` | ✅ 0 warnings |
| `npm run test` | ✅ 158 tests passed |

### Files

- `cloudbuild.yaml` — created
- `src/config.ts` — updated
- `src/api/routes/interview.ts` — updated
- `src/api/routes/job.ts` — updated
- `src/utils/__tests__\config.test.ts` — updated
- `docs/deployment/secrets-setup.md` — created
- `docs/deployment/deploy.sh` — created
