# Fix responsive font: 18px base, 16px tags

## Decision
- 18px = base size everywhere (chat, inputs, sections)
- 16px = for tags (topic labels)

## Files

### `packages/web/components/MessageBubble.tsx`
- Topic tag: `text-xs` → `text-sm` (12→16)

### `packages/web/components/SummaryView.tsx`
- Section headings: `text-lg sm:text-lg` → `text-lg` (remove sm, 18px everywhere)
- Section content: `text-lg sm:text-base` → `text-lg` (remove sm, 18px everywhere)
- Score labels: `text-[15px] sm:text-sm` → `text-sm sm:text-lg` (16→18)
- Question N/M: `text-lg sm:text-lg` → `text-lg` (remove sm)
- Tabs: `text-[15px] sm:text-sm` → `text-sm sm:text-lg` (16→18)

### `packages/web/components/ChatWindow.tsx`
- Feedback: `text-[15px] sm:text-sm` → `text-sm sm:text-lg` (16→18)

## Verification
1. `npm run typecheck`
2. `npm run lint`
3. `npm run test`
